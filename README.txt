README:

Use python3 main.py RNN to run the base multichannel RNN model 

Use python3 main.py RNN_DROPOUT to run the multichannel RNN model

Use python3 main.py RNN_ALL to run the multichannel multi-input RNN model with integrated NLP processing and numerical analysis